<p style="color: green"><%= notice %></p>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to "Show this item", board_item_url(item.board, item) %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_board_item_path %>
