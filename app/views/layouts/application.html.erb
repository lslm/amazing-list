<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title>AmazingList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-16">
            <div class="flex items-center">
              <div class="hidden md:block">
                <div class="ml-10 flex items-baseline space-x-4">
                  <%= link_to 'Minhas listas', boards_path, class: 'bg-gray-900 text-white px-3 py-2 rounded-md text-sm font-medium' %>
                </div>
              </div>
            </div>
            <div class="hidden md:block">
              <div class="ml-4 flex items-center md:ml-6">
                <div data-controller="menu" class="ml-3 relative">
                  <% if user_signed_in? %>
                    <div data-action="click->menu#toggle">
                      <button type="button" class="max-w-xs bg-gray-800 text-white rounded-full flex items-center text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
                        <%= current_user.name %>
                      </button>
                    </div>
                  <% else %>
                    <%= link_to "Log in", new_session_path(resource_name), class: "text-white px-3 py-2 rounded-md text-sm font-medium" %>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
      <main>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
